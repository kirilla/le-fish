@page "/remove-attack-event/{id:int}"
@model RemoveAttackEventModel
@{
}

@section Meta {
    <title>
        Ta bort sidbesök -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Ta bort sidbesöket:<br />
        @(Model.AttackEvent.TargetName)
        @(Model.AttackEvent.TargetAddress)<br />
        @(Model.AttackEvent.PageName)<br />
        @(Model.AttackEvent.Created.ToFixedFormatDateShortTime())
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.Id" />

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Ta bort
        </button>

        <a class="button" href="/show-attack-event/@(Model.AttackEvent.Id)">
            Avbryt
        </a>
    </form>

</main>

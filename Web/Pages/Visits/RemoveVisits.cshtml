@page "/remove-page-visits"
@model RemoveVisitsModel
@{
}

@section Meta {
    <title>
        Ta bort sidbesök -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Ta bort sidbesök
    </h4>

    <form method="post">

        <label asp-for="CommandModel.EmailTargetId">
            Mål
        </label>

        <select asp-for="CommandModel.EmailTargetId">
            <option value="">–</option>
            @foreach (var target in Model.EmailTargets)
            {
                <option value="@(target.Id)">
                    @(target.Name), @(target.Address)
                </option>
            }
        </select>

        <p>
            Lämna tomt för att ta bort alla.
        </p>

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Ta bort
        </button>

        <a class="button" href="/show-page-visits">
            Avbryt
        </a>

    </form>

</main>

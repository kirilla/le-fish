@page "/remove-target-instruction/{id:int}"
@model RemoveTargetInstructionModel
@{
}

@section Meta {
    <title>
        Ta bort instruktion -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Ta bort instruktionen:
        @(Model.TargetInstruction.Name)
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.TargetInstructionId" />

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Ta bort
        </button>

        <a class="button" href="/show-target-instruction/@(Model.TargetInstruction.Id)">
            Avbryt
        </a>
    </form>

</main>

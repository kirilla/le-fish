@page "/show-data-dump/{id:int}"
@model ShowDataDumpModel
@{
}

@section Meta {
    <title>Datadump - @(Branding.WebsiteTitle)</title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>

    <h4>
        Datadump
    </h4>

    <div class="notice">
        <p>
            Skapad: @(Model.DataDump.Created.ToFixedFormatDateShortTime())
        </p>
    </div>

    <pre class="notice">@(Model.DataDump.JsonData)</pre>

    <p>
        @if (Model.CanEditDataDump)
        {
            <a class="button" href="/edit-data-dump/@(Model.DataDump.Id)">
                Redigera
            </a>
        }

        @if (Model.CanRemoveDataDump)
        {
            <a class="button" href="/remove-data-dump/@(Model.DataDump.Id)">
                Ta bort
            </a>
        }
    </p>

</main>

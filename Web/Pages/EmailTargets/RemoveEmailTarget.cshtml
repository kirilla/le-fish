@page "/remove-email-target/{id:int}"
@model RemoveEmailTargetModel
@{
}

@section Meta {
    <title>
        Ta bort målkonto -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Ta bort målkontot:
        @(Model.EmailTarget.Address)
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.EmailTargetId" />

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Ta bort
        </button>

        <a class="button" href="/show-email-target/@(Model.CommandModel.EmailTargetId)">
            Avbryt
        </a>
    </form>

</main>

@page "/show-payload-page-targets/{id:int}"
@model ShowPayloadPageTargetsModel
@{
}

@section Meta {
    <title>Fångstsida - @(Branding.WebsiteTitle)</title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>

    <h4>
        🕸️ Fångstsida: 
        @(Model.PayloadPage.PageKey)
    </h4>

    <h5>
        URL
    </h5>
    <p class="notice">
        /page/@(Model.PayloadPage.PageKey)/[[target_key]]
    </p>

    <h5>
        Besök sidan som...
    </h5>

    <ul class="notice">
        @foreach (var target in Model.EmailTargets)
        {
            var url = $"/page/{Model.PayloadPage.PageKey}/{target.PersonKey}";
            <li>
                🎯
                &thinsp;
                <a href="@(url)">@(url)</a>
                &ensp;
                @(target.Address)
            </li>
        }

        @if (!Model.EmailTargets.Any())
        {
            <li>
                🎯
                &thinsp;
                &ndash;
            </li>
        }
    </ul>

</main>

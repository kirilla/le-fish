@page "/edit-ip-range/{id:int}"
@model EditIpRangeModel
@{
}

@section Meta {
    <title>
        Redigera IP-adressintervall -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Redigera IP-adressintervallet:
        @(Model.IpRange.Range)
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.Id" />

        <input asp-for="CommandModel.Range" placeholder="192.168.0.1/32" />

        <p>
            <label asp-for="CommandModel.Blocked">
                <input asp-for="CommandModel.Blocked" type="checkbox" />
                Blockerad
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Spara
        </button>

        <a class="button" href="/show-ip-range/@(Model.IpRange.Id)">
            Avbryt
        </a>
    </form>

</main>

@page "/remove-page-visit/{id:int}"
@model RemovePageVisitModel
@{
}

@section Meta {
    <title>
        Ta bort sidbesök -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Ta bort sidbesöket:<br />
        @(Model.PageVisit.TargetName)
        @(Model.PageVisit.TargetAddress)<br />
        @(Model.PageVisit.PageName)<br />
        @(Model.PageVisit.Created.ToFixedFormatDateShortTime())
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.PageVisitId" />

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Ta bort
        </button>

        <a class="button" href="/show-page-visit/@(Model.PageVisit.Id)">
            Avbryt
        </a>
    </form>

</main>

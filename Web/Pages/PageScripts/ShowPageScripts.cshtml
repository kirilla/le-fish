@page "/show-page-scripts"
@model ShowPageScriptsModel
@{
}

@section Meta {
    <title>
        Skript -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>

    <h4>
        Skript
    </h4>

    <div class="notice">

        <ul>
            @foreach (var page in Model.PageScripts)
            {
                <li>
                    📜
                    &thinsp;
                    <a href="/show-page-script/@(page.Id)">@(page.Name)</a>
                </li>
            }

            @if (!Model.PageScripts.Any())
            {
                <li>
                    📜
                    &thinsp;
                    &ndash;
                </li>
            }
        </ul>

    </div>

    @if (Model.CanAddPageScript)
    {
        <p>
            <a class="button" href="/add-page-script">
                Lägg till ett skript
            </a>
        </p>
    }

</main>

@page "/clone-payload-script/{id:int}"
@model ClonePageScriptModel
@{
}

@section Meta {
    <title>
        Klona skript -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Klona skriptet:
        @(Model.PageScript.Name)
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.Id" />

        <label asp-for="CommandModel.Name">
            Namn
        </label>
        <input asp-for="CommandModel.Name" />

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Klona
        </button>

        <a class="button" href="/show-payload-script/@(Model.PageScript.Id)">
            Avbryt
        </a>
    </form>

</main>

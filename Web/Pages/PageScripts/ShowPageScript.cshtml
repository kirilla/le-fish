@page "/show-page-script/{id:int}"
@model ShowPageScriptModel
@{
}

@section Meta {
    <title>Skript - @(Branding.WebsiteTitle)</title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>

    <h4>
        📜
        @(Model.PageScript.Name)
    </h4>

    <h5>
        Skript
    </h5>
    <pre class="notice">@(Model.PageScript.Script)</pre>

    <p>
        @if (Model.CanClonePageScript)
        {
            <a class="button" href="/clone-page-script/@(Model.PageScript.Id)">
                Klona
            </a>
        }

        @if (Model.CanEditPageScript)
        {
            <a class="button" href="/edit-page-script/@(Model.PageScript.Id)">
                Redigera
            </a>
        }

        @if (Model.CanRemovePageScript)
        {
            <a class="button" href="/remove-page-script/@(Model.PageScript.Id)">
                Ta bort
            </a>
        }
    </p>

</main>

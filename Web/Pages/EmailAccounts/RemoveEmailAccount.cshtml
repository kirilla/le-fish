@page "/remove-email-account/{id:int}"
@model RemoveEmailAccountModel
@{
}

@section Meta {
    <title>
        Ta bort epostkonto -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Ta bort epostkontot:
        @(Model.EmailAccount.FromName)
        @(Model.EmailAccount.FromAddress)
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.EmailAccountId" />

        <p>
            <label asp-for="CommandModel.Confirmed">
                <input asp-for="CommandModel.Confirmed" type="checkbox" />
                Ja, jag är säker
            </label>
        </p>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Ta bort
        </button>

        <a class="button" href="/show-email-account/@(Model.CommandModel.EmailAccountId)">
            Avbryt
        </a>
    </form>

</main>

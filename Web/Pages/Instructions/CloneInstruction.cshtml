@page "/clone-instruction/{id:int}"
@model CloneInstructionModel
@{
}

@section Meta {
    <title>
        Klona instruktion -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Klona instruktionen:
        @(Model.Instruction.Name)
    </h4>

    <form method="post">

        <input type="hidden" asp-for="CommandModel.InstructionId" />

        <label asp-for="CommandModel.Name">
            Namn
        </label>
        <input asp-for="CommandModel.Name" />

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Klona
        </button>

        <a class="button" href="/show-instruction/@(Model.Instruction.Id)">
            Avbryt
        </a>
    </form>

</main>

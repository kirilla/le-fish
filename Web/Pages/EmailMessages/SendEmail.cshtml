@page "/send-email"
@model SendEmailModel
@{
}

@section Meta {
    <title>
        Skicka meddelande -
        @(Branding.WebsiteTitle)
    </title>
}

@section Header {
    <partial name="_Navbar" model="Model.UserToken" />
}

<main>
    <h4>
        Skicka meddelande
    </h4>

    <form method="post">

        <label asp-for="CommandModel.EmailAccountId">
            Avsändare
        </label>

        <select asp-for="CommandModel.EmailAccountId">
            <option value="">–</option>
            @foreach (var target in Model.EmailAccounts)
            {
                <option value="@(target.Id)">
                    @(target.FromName), @(target.FromAddress)
                </option>
            }
        </select>

        <label asp-for="CommandModel.TargetId">
            Mottagare
        </label>

        <select asp-for="CommandModel.TargetId">
            <option value="">–</option>
            @foreach (var target in Model.Targets)
            {
                <option value="@(target.Id)">
                    @(target.Name), @(target.Address)
                </option>
            }
        </select>

        <label asp-for="CommandModel.EmailTemplateId">
            Epostmall
        </label>

        <select asp-for="CommandModel.EmailTemplateId">
            <option value="">–</option>
            @foreach (var target in Model.EmailTemplates)
            {
                <option value="@(target.Id)">
                    @(target.Subject)
                </option>
            }
        </select>

        <label asp-for="CommandModel.WebPageId">
            Länk till sida
        </label>

        <select asp-for="CommandModel.WebPageId">
            <option value="">–</option>
            @foreach (var page in Model.WebPages)
            {
                <option value="@(page.Id)">
                    @(page.Name)
                </option>
            }
        </select>

        <label asp-for="CommandModel.PageScriptId">
            Skript på sidan
        </label>

        <select asp-for="CommandModel.PageScriptId">
            <option value="">–</option>
            @foreach (var script in Model.PageScripts)
            {
                <option value="@(script.Id)">
                    @(script.Name)
                </option>
            }
        </select>

        <div asp-validation-summary="All"></div>

        <button type="submit">
            Skicka
        </button>

        <a class="button" href="/attack">
            Avbryt
        </a>
    </form>

</main>
